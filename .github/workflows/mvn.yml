name: Maven Runner
on:
  push:
    branches: [ bshp/master ]
  pull_request:
    branches: [ bshp/master ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Maven CLI Runner (mvn)
        uses: bshp/actions-mvn@master
